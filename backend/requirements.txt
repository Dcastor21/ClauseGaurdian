# ClauseGuardian backend dependencies
# Install with: pip install -r requirements.txt
# Python 3.11+ required

# ── WEB FRAMEWORK ─────────────────────────────────────────────────────────────
fastapi>=0.115.0           # async web framework — the core of the API
uvicorn[standard]>=0.32.0  # ASGI server that runs FastAPI; [standard] adds websocket + reload support
python-multipart>=0.0.12   # required for file upload form parsing (UploadFile in FastAPI)

# ── CONFIGURATION ─────────────────────────────────────────────────────────────
pydantic>=2.0.0             # data validation — used for all models
pydantic-settings>=2.0.0    # reads .env files into Pydantic Settings models

# ── DATABASE ──────────────────────────────────────────────────────────────────
supabase>=2.0.0             # Supabase Python client — wraps PostgREST and Storage APIs

# ── AUTH — CLERK JWT VALIDATION ───────────────────────────────────────────────
PyJWT>=2.8.0                # JWT decode and verification
cryptography>=42.0.0        # required by PyJWT for RS256 asymmetric key handling
svix>=1.0.0                 # Clerk's webhook delivery library — verifies Svix HMAC signatures

# ── HTTP CLIENT ───────────────────────────────────────────────────────────────
httpx>=0.27.0               # async HTTP client — used for JWKS fetching and outbound API calls

# ── LLM PIPELINE ─────────────────────────────────────────────────────────────
langchain>=0.3.0                 # LLM orchestration framework
langchain-openai>=0.2.0          # LangChain's OpenAI-compatible adapter (used for OpenRouter)
langchain-text-splitters>=0.3.0  # RecursiveCharacterTextSplitter for chunking
tiktoken>=0.7.0                  # OpenAI tokenizer — used by RecursiveCharacterTextSplitter for exact token counts

# ── DOCUMENT PARSING ──────────────────────────────────────────────────────────
pypdf>=4.0.0                # PDF text extraction — page-by-page with page number tracking
python-docx>=1.1.0          # DOCX text extraction — paragraph by paragraph

# ── SCHEDULING ────────────────────────────────────────────────────────────────
apscheduler>=3.10.0         # background job scheduler for deadline alerts

# ── ALERTS ────────────────────────────────────────────────────────────────────
resend>=2.0.0               # transactional email via Resend API

# ── RATE LIMITING ─────────────────────────────────────────────────────────────
upstash-redis>=1.0.0        # Upstash Redis REST client for rate limiting

# ── MONITORING ────────────────────────────────────────────────────────────────
sentry-sdk[fastapi]>=2.0.0  # error tracking; [fastapi] adds automatic request context

# ── TESTING ───────────────────────────────────────────────────────────────────
pytest>=8.0.0               # test runner
pytest-asyncio>=0.23.0      # async test support (needed for async FastAPI endpoints)
httpx>=0.27.0               # also used by TestClient for endpoint testing
coverage>=7.0.0             # code coverage reporting (70% minimum gate in CI)
